# دليل شرح المشروع — نظام محاكاة تداول فائض الطاقة الشمسية
### موجّه للطالب لشرح المشروع أمام المشرف ولجنة المناقشة

---

## كيف تستخدم هذا الدليل

اقرأ كل قسم بعناية. كل قسم يشرح **لماذا** اتُّخذ القرار، **وماذا** تم بناؤه، **وكيف** يعمل الكود. الهدف أن تستطيع شرح المشروع بثقة أمام أي شخص حتى لو لم يكن متخصصاً في البرمجة.

---

# الجزء الأول — فكرة المشروع وسبب بنائه

## القسم 1: ما هي المشكلة التي يحلها المشروع؟

### الشرح للمشرف واللجنة:
> "المشكلة الأساسية هي أن المنازل التي تمتلك ألواحاً شمسية تنتج كهرباء أكثر مما تستهلك في أوقات كثيرة — خاصةً في منتصف النهار. هذا الفائض إما يُهدر وإما يُرسَل للشبكة العامة بلا مقابل. فكرة المشروع هي: لماذا لا يبيع صاحب المنزل هذا الفائض مباشرةً لجاره الذي يحتاج كهرباء؟"

### المصطلح العلمي:
هذا يُسمى **"التداول بين الأقران" أو P2P Energy Trading** — أي تداول الطاقة مباشرةً بين المنتجين والمستهلكين دون وسيط مركزي.

### لماذا هو محاكاة (Simulation) وليس نظاماً حقيقياً؟
لأن تنفيذه على أرض الواقع يتطلب بنية تحتية فيزيائية (عدادات ذكية، شبكة كهرباء معدّلة، ترخيص حكومي). الرسالة تُثبت الجدوى النظرية والبرمجية فقط.

---

## القسم 2: الهيكل العام للمشروع

### مكونات النظام الأربعة:

```
┌─────────────────────────────────────────────────────┐
│                    المستخدم (المتصفح)               │
│            React.js — واجهة المستخدم                │
└───────────────────────┬─────────────────────────────┘
                        │ يتواصل مع
┌───────────────────────▼─────────────────────────────┐
│                   Firebase                           │
│  ┌─────────────────┐   ┌────────────────────────┐   │
│  │  Authentication │   │  Cloud Firestore        │   │
│  │  (تسجيل الدخول) │   │  (قاعدة البيانات)       │   │
│  └─────────────────┘   └────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**لا يوجد سيرفر خلفي (Backend Server)** — Firebase يقوم بكل شيء مباشرةً من المتصفح. هذا يُبسّط البنية ويجعل التطبيق أسرع.

---

# الجزء الثاني — قرارات التصميم التقني

## القسم 3: لماذا اخترنا React.js؟

### الشرح البسيط:
React هي مكتبة JavaScript لبناء واجهات المستخدم. اخترناها لثلاثة أسباب:

**1. المكونات (Components):**
بدلاً من كتابة صفحة HTML كاملة في ملف واحد، نكتب "قطعاً" صغيرة مستقلة يمكن إعادة استخدامها. مثلاً: `StatsCard` هو مكوّن واحد نستخدمه في عدة أماكن.

```jsx
// مثال: نفس المكوّن يُعرض 6 مرات في صفحة الملف الشخصي
<StatsCard label="الرصيد الحالي"    value="14.5 kWh" />
<StatsCard label="إجمالي الإنتاج"   value="980.0 kWh" />
<StatsCard label="رصيد المحفظة"     value="350.00 د.ل" />
```

**2. التحديث التلقائي (Reactivity):**
عندما تتغير البيانات في قاعدة البيانات (مثلاً بعد عملية شراء)، تتحدث الشاشة تلقائياً دون إعادة تحميل الصفحة.

**3. React Router — التنقل بين الصفحات:**
يجعل التطبيق يعمل كتطبيق موبايل — تنتقل بين الصفحات بسرعة دون أن تُعاد تحميل كل شيء.

---

## القسم 4: لماذا اخترنا Firebase؟

Firebase هي خدمة من Google توفر:

| الخدمة | الاستخدام في المشروع |
|--------|---------------------|
| **Authentication** | تسجيل الدخول والخروج وإنشاء الحسابات |
| **Cloud Firestore** | قاعدة البيانات الرئيسية (تحديثات فورية) |

### الميزة الأهم — التحديث الفوري (Real-time):
Firestore يستخدم تقنية `onSnapshot` — أي أن التطبيق "يستمع" لقاعدة البيانات. في اللحظة التي يتغير فيها رصيدك بعد بيع طاقة، تظهر التغيير فوراً على شاشتك وعلى شاشة المشتري في نفس الوقت.

```javascript
// هذا الكود في AuthContext.jsx يستمع لتغييرات بيانات المنزل
onSnapshot(homeRef, (snap) => {
  setHomeData(snap.data()); // يُحدّث الشاشة تلقائياً عند أي تغيير
});
```

---

## القسم 5: لماذا اخترنا Tailwind CSS؟

بدلاً من كتابة ملفات CSS منفصلة، Tailwind يتيح لنا كتابة التصميم مباشرةً في HTML/JSX باستخدام كلاسات جاهزة:

```jsx
// بدون Tailwind (معقّد):
<div style={{ backgroundColor: '#1e293b', borderRadius: '16px', padding: '20px' }}>

// مع Tailwind (بسيط وسريع):
<div className="bg-dark-800 rounded-2xl p-5">
```

أضفنا ألواناً مخصصة للمشروع في `tailwind.config.js`:
- **solar** (ذهبي/كهرماني) — اللون الرئيسي لأي شيء متعلق بالطاقة الشمسية
- **dark** (ألوان داكنة متدرجة) — للخلفيات والبطاقات

---

# الجزء الثالث — بناء قاعدة البيانات

## القسم 6: تصميم قاعدة البيانات (Firestore)

Firestore قاعدة بيانات من نوع **NoSQL** — تخزّن البيانات كوثائق (Documents) داخل مجموعات (Collections)، مثل ملفات داخل مجلدات.

### المجموعة الأولى: `/homes`
تخزّن بيانات كل منزل. كل وثيقة اسمها هو الـ UID الخاص بالمستخدم من Firebase Auth.

```
homes/
├── uid_abc123/          ← منزل هشام
│   ├── name: "منزل الشمس"
│   ├── balance: 14.5          (كيلوواط متاح للبيع)
│   ├── walletBalance: 350.00  (دينار ليبي في المحفظة)
│   ├── meterRef: "MTR-AJ-10001"
│   └── isAdmin: true
│
└── uid_def456/          ← منزل آخر
    ├── name: "منزل النور"
    ├── balance: 8.0
    └── walletBalance: 210.50
```

### المجموعة الثانية: `/offers`
عروض البيع المفتوحة في السوق.

```
offers/
└── offer_xyz/
    ├── sellerUid: "uid_abc123"
    ├── amount: 5.0         (كيلوواط للبيع)
    ├── pricePerKwh: 2.50   (دينار لكل كيلوواط)
    └── status: "open"      (open / completed / cancelled)
```

### المجموعة الثالثة: `/transactions` — سجل المعاملات (لا يُمسح أبداً)
```
transactions/
└── tx_qrs/
    ├── sellerName: "منزل الشمس"
    ├── buyerName: "منزل النور"
    ├── kwh: 3.0
    ├── pricePerKwh: 2.50
    ├── total: 7.50   (دينار)
    └── paymentReference: "PAY-ABC123-XYZ"
```

### المجموعة الفرعية: `/homes/{uid}/dailyStats`
إحصائيات الإنتاج اليومي لكل منزل.

---

# الجزء الرابع — بناء نظام المصادقة (Authentication)

## القسم 7: كيف يعمل تسجيل الدخول؟

### الملف: `src/context/AuthContext.jsx`

هذا الملف هو "قلب" نظام المصادقة. يعمل عبر نمط تصميم يُسمى **Context API** — يجعل بيانات المستخدم متاحةً لكل مكوّن في التطبيق دون تمريرها يدوياً.

```
التطبيق بالكامل
    └── AuthProvider (AuthContext.jsx)
            ├── يستمع لحالة تسجيل الدخول
            ├── إذا مسجّل دخول → يجلب بيانات منزله من Firestore
            └── يوفّر: currentUser, homeData, signOut
                        ↓
            أي صفحة تحتاج المعلومات تستدعي:
            const { currentUser, homeData } = useAuth();
```

### الكود الأساسي:
```javascript
// عندما يفتح التطبيق، Firebase يتحقق هل هناك جلسة نشطة
onAuthStateChanged(auth, (user) => {
  if (user) {
    // المستخدم مسجّل دخول → اجلب بياناته من Firestore
    onSnapshot(doc(db, 'homes', user.uid), (snap) => {
      setHomeData(snap.data());
    });
  }
});
```

## القسم 8: كيف يعمل التسجيل (إنشاء حساب جديد)؟

### الملف: `src/pages/RegisterPage.jsx` + `src/services/homeService.js`

خطوات إنشاء الحساب:

```
1. المستخدم يُدخل: اسم المنزل، الإيميل، الهاتف، كلمة المرور
          ↓
2. التحقق من صحة المدخلات:
   - هل الهاتف ليبي صحيح؟ (يبدأ بـ 09 ويتبعه 8 أرقام)
   - هل كلمة المرور 6 أحرف على الأقل؟
   - هل كلمتا المرور متطابقتان؟
          ↓
3. إنشاء حساب في Firebase Auth (createUserWithEmailAndPassword)
   → Firebase يعطيه UID فريد تلقائياً
          ↓
4. كتابة وثيقة في Firestore /homes/{uid}:
   - رصيد محفظة ابتدائي: 100 دينار ليبي
   - رصيد طاقة: 0 كيلوواط
   - رقم عداد تلقائي: MTR-AJ-XXXXX
          ↓
5. Firebase يسجّل دخوله تلقائياً → يُحوَّل للوحة التحكم
```

### التحقق من رقم الهاتف الليبي (الملف: `src/utils/meter.js`):
```javascript
// يقبل: 0912345678 أو +2189XXXXXXXX
function isValidLibyanMobile(value) {
  return /^09\d{8}$/.test(value) || /^\+2189\d{8}$/.test(value);
}
```

### توليد رقم العداد التلقائي:
```javascript
// ينتج: "MTR-AJ-04821"
function generateMeterRef(regionCode = 'AJ') {
  const digits = Math.floor(10000 + Math.random() * 90000);
  return `MTR-${regionCode}-${digits}`;
}
```

---

# الجزء الخامس — محرك التداول الذري

## القسم 9: ما هي "الذرية" (Atomicity) ولماذا هي ضرورية؟

### المشكلة التي تحلها:
تخيّل أن هشاماً يشتري 5 كيلوواط من عمر. العملية تتطلب خطوات متعددة:
1. خصم 5 كيلوواط من رصيد عمر
2. إضافة 5 كيلوواط لرصيد هشام
3. خصم المبلغ بالدينار من محفظة هشام
4. إضافة المبلغ لمحفظة عمر
5. تحديث حالة العرض
6. كتابة سجل المعاملة

**الخطر:** ماذا لو انقطع الإنترنت بعد الخطوة 1 ولم تُنفَّذ بقية الخطوات؟ سيخسر عمر الطاقة بدون مال!

**الحل — writeBatch:** كل الخطوات تُنفَّذ دفعةً واحدة. إما **كلها معاً** أو **لا شيء منها** — لا يوجد حالة وسطى.

```javascript
// هذا في tradeService.js
const batch = writeBatch(db);

batch.update(sellerRef, { balance: increment(-amount) });   // خصم من البائع
batch.update(buyerRef,  { balance: increment(+amount) });   // إضافة للمشتري
batch.update(offerRef,  { status: 'completed' });           // تحديث العرض
batch.set(txRef, { ...بيانات_المعاملة });                    // تسجيل في السجل

await batch.commit(); // كل ما سبق يُنفَّذ في لحظة واحدة
```

## القسم 10: الخطوات الثماني لعملية التداول

هذا الكود في `src/services/tradeService.js` — دالة `executeEnergyTrade()`:

```
الخطوة 1 — التحقق من المدخلات:
  هل الكمية أكبر من صفر؟ هل العرض موجود؟

الخطوة 2 — جلب بيانات البائع والمشتري من Firestore

الخطوة 3 — التحقق من الأرصدة:
  هل لدى البائع كيلوواط كافية؟
  هل لدى المشتري دينار كافٍ؟

الخطوة 4 — إنشاء writeBatch (الحاوية الذرية)

الخطوة 5 — كتابة التحديثات في الـ batch:
  • خصم kWh من البائع + إضافتها للمشتري
  • خصم دينار من المشتري + إضافته للبائع
  • تحديث إحصائيات الطرفين (totalSold, totalPurchased)

الخطوة 6 — تحديث العرض:
  • إذا اشترى الكمية كاملة → status = 'completed'
  • إذا اشترى جزءاً → تقليل الكمية المتبقية (partial fill)

الخطوة 7 — كتابة سجل المعاملة الدائم في /transactions
  (لا يُمكن تعديله أو حذفه أبداً — مثل سجل محاسبة رسمي)

الخطوة 8 — batch.commit() — تنفيذ كل شيء دفعةً واحدة
```

---

# الجزء السادس — نموذج المحاكاة الشمسية

## القسم 11: المعادلة الرياضية لإنتاج الطاقة

### المعادلة الأساسية (من الفصل الثالث للرسالة):

```
P(t) = P_peak × η × I(t) / I_STC
```

| الرمز | القيمة | المعنى |
|-------|--------|--------|
| P_peak | 3.0 kWp | القدرة الذروية للألواح المركّبة |
| η (إيتا) | 0.80 | كفاءة النظام (80%) |
| I(t) | 0.65 – 0.95 | شدة الإشعاع الشمسي (عشوائي) |
| I_STC | 1.0 kW/m² | الإشعاع في ظروف الاختبار المعيارية |

### الملف: `src/services/productionService.js`

**كيف تعمل المحاكاة اليومية؟**

```javascript
// نأخذ عينة لكل ساعة من ساعات اليوم (0 → 23)
for (let h = 0; h < 24; h++) {
  // شكل الإشعاع: نصف دائرة بين الشروق (6ص) والغروب (6م)
  // إشعاع = صفر خارج ساعات النهار
  const shape = Math.sin(((h - 6) / 12) * Math.PI);

  // العامل الموسمي: يبلغ أعلاه يوم الانقلاب الصيفي (21 يونيو)
  const seasonal = 0.95 + 0.20 * Math.cos(phase);

  // الإشعاع العشوائي يحاكي تقلبات السحاب
  const I_t = random(0.65, 0.95) × seasonal × shape;

  // تطبيق المعادلة: P(t) = Ppeak × η × I(t) / I_STC
  production += (3.0 × 0.80 × I_t) / 1.0;
}
```

**الاستهلاك:** عشوائي بين 5 و 15 كيلوواط/يوم (يمثّل الاستهلاك المنزلي العادي في أجدابيا)

**الفائض:** = max(0, الإنتاج − الاستهلاك) ← يُضاف لرصيد الطاقة

---

# الجزء السابع — بناء الواجهة

## القسم 12: هيكل صفحات التطبيق

```
التطبيق
├── صفحات عامة (بدون تسجيل دخول):
│   ├── /login     — تسجيل الدخول
│   └── /register  — إنشاء حساب
│
└── صفحات محمية (تتطلب تسجيل دخول):
    ├── /dashboard  — لوحة التحكم الرئيسية
    ├── /sell       — بيع الطاقة
    ├── /buy        — شراء الطاقة
    ├── /history    — سجل المعاملات
    ├── /profile    — الملف الشخصي
    └── /admin      — لوحة الإدارة (للمشرف فقط)
```

## القسم 13: كيف تُحمى الصفحات من الدخول بدون تسجيل؟

### الملف: `src/components/PrivateRoute.jsx`

```javascript
// إذا لم يكن مسجّلاً دخوله → أرسله لصفحة تسجيل الدخول
function PrivateRoute() {
  const { currentUser } = useAuth();
  return currentUser ? <Outlet /> : <Navigate to="/login" />;
}
```

```jsx
// في App.js — كل الصفحات المحمية داخل PrivateRoute
<Route element={<PrivateRoute />}>
  <Route path="/dashboard" element={<Dashboard />} />
  <Route path="/sell"      element={<SellEnergy />} />
  ...
</Route>
```

## القسم 14: التصميم المتجاوب (Responsive Design)

التطبيق يعمل على الموبايل والكمبيوتر بشكل مختلف:

```
على الكمبيوتر (شاشة ≥ 1024px):
┌──────────────────────────────────┐
│ القائمة الجانبية  │  محتوى الصفحة │
│  (Sidebar)       │               │
│  • اسم المنزل    │               │
│  • الرصيد        │               │
│  • روابط التنقل  │               │
└──────────────────────────────────┘

على الموبايل (شاشة < 1024px):
┌──────────────────────────┐
│  رأس الصفحة (Header)    │  ← اسم المنزل + الأرصدة
├──────────────────────────┤
│                          │
│     محتوى الصفحة         │
│                          │
├──────────────────────────┤
│  شريط التنقل السفلي      │  ← 5 أيقونات: الرئيسية، بيع، شراء، السجل، الملف
└──────────────────────────┘
```

هذا يتحقق في Tailwind بكتابة: `hidden lg:flex` (مخفي على موبايل، ظاهر على كمبيوتر).

## القسم 15: الصور الرمزية (الأفاتار)

بدلاً من رفع صورة، كل منزل يحصل على دائرة ملونة بأول حرف من اسمه. اللون يُحسب رياضياً من الاسم (نفس الاسم = نفس اللون دائماً):

```javascript
// في utils/format.js
const COLORS = ['#f59e0b', '#3b82f6', '#10b981', '#8b5cf6', ...];

function getAvatarColor(name) {
  let hash = 0;
  for (const c of name) hash = (hash << 5) - hash + c.charCodeAt(0);
  return COLORS[Math.abs(hash) % COLORS.length]; // نفس الاسم = نفس اللون
}
```

---

# الجزء الثامن — الأمان وقواعد الوصول

## القسم 16: قواعد أمان Firestore

الملف: `firestore.rules` — يتحكم في من يستطيع قراءة/كتابة ماذا.

### المبدأ الأساسي:
```
كل مستخدم مسجّل:
  ✅ يمكنه قراءة بيانات أي منزل (لعرض أسماء البائعين في السوق)
  ✅ يمكنه إنشاء منزله الخاص فقط
  ✅ يمكنه تحديث أي منزل (ضروري لمحرك التداول الذري)
  ❌ لا يمكنه حذف أي منزل

سجلات المعاملات:
  ✅ يمكن للمشتري أو البائع فقط قراءة معاملاتهم
  ✅ يمكن كتابة معاملة جديدة فقط
  ❌ لا يمكن تعديل أو حذف أي معاملة أبداً (سجل محاسبة دائم)
```

## القسم 17: حماية المفاتيح السرية

Firebase يعطيك مفاتيح API للتواصل معه. هذه المفاتيح يجب أن تبقى سرية:

```
.env  ← ملف يحتوي المفاتيح السرية
```

هذا الملف **لا يُرفع أبداً على GitHub** لأن:
- GitHub عام وأي شخص يستطيع رؤيته
- لو وجد أحد مفاتيحك، يمكنه استخدام قاعدة بياناتك بلا إذن

**الحل:** وضع اسم الملف في `.gitignore` — أمر يخبر Git بتجاهله تماماً.

```
# .gitignore
.env
scripts/serviceAccountKey.json
```

---

# الجزء التاسع — نشر التطبيق

## القسم 18: كيف يعمل النشر؟

لرفع التطبيق على الإنترنت، نمر بخطوتين:

**الخطوة 1 — البناء (Build):**
```bash
npm run build
```
يحوّل كل ملفات React إلى HTML + CSS + JavaScript بسيطة يفهمها المتصفح ويضغطها لتكون أسرع.

**الخطوة 2 — الرفع:**
- **Firebase Hosting:** `firebase deploy`
- **Vercel:** يكفي رفع الكود على GitHub وVercel يبني وينشر تلقائياً

### ملف `vercel.json` — يحل مشكلة التنقل:
```json
{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```
بدونه، لو فتح المستخدم رابطاً مثل `/dashboard` مباشرةً سيحصل على خطأ 404. هذا الملف يوجّه كل الروابط لصفحة index.html ويتركها لـ React Router يتعامل معها.

---

# الجزء العاشر — البيانات التجريبية

## القسم 19: سكريبت الزرع (Seed Script)

الملف: `scripts/seedFirestore.js`

لاختبار التطبيق، نحتاج حسابات جاهزة بدلاً من إنشائها يدوياً. هذا السكريبت:

1. يتصل بـ Firebase باستخدام مفتاح الإدارة (serviceAccountKey.json)
2. يُنشئ 5 حسابات في Firebase Auth تلقائياً
3. يكتب وثيقة لكل حساب في Firestore بالبيانات المحددة

```
home1@solar.ly — منزل الشمس — أجدابيا  ← المشرف (isAdmin: true)
home2@solar.ly — منزل النور — بنغازي
home3@solar.ly — منزل الأمل — طبرق
home4@solar.ly — منزل الفجر — درنة
home5@solar.ly — منزل السلام — الكفرة

كلمة المرور لجميعهم: Solar@2024
```

---

# الجزء الحادي عشر — رحلة المستخدم الكاملة

## القسم 20: كيف تبدو تجربة المستخدم خطوةً بخطوة

```
1. يفتح التطبيق → يرى شاشة تسجيل الدخول
        ↓
2. يُدخل إيميله وكلمة المرور → Firebase يتحقق
        ↓
3. يُحوَّل للوحة التحكم (Dashboard):
   • يرى رصيده الحالي (كيلوواط + دينار)
   • يرى رسم بياني لإنتاجه على مدار الأيام
   • يرى أحدث العروض المفتوحة في السوق
        ↓
4. يريد إضافة رصيد طاقة → يذهب للملف الشخصي:
   • يضغط "محاكاة إنتاج اليوم"
   • النموذج الرياضي يحسب الإنتاج والاستهلاك
   • الفائض يُضاف لرصيده تلقائياً
        ↓
5. يريد بيع الفائض → يذهب لصفحة "بيع الطاقة":
   • يُدخل الكمية (كيلوواط) والسعر (د.ل/كيلوواط)
   • العرض يظهر في السوق فوراً
        ↓
6. مستخدم آخر يرى العرض في "شراء الطاقة":
   • يختار الكمية التي يريد شراءها
   • يضغط "شراء" → تتم الصفقة الذرية الثمانية الخطوات
   • الأرصدة تتحدث فوراً لكلا الطرفين
        ↓
7. كلاهما يجد سجل الصفقة في "سجل المعاملات"
   مع رقم مرجعي فريد: PAY-XXXXX-YYYYY
```

---

# الجزء الثاني عشر — أسئلة اللجنة المحتملة وإجاباتها

## القسم 21: أسئلة شائعة وكيف تجيب عليها

---

**س: لماذا اخترت Firebase بدلاً من بناء باك-إند خاص (مثل Node.js أو Python)?**

ج: Firebase يوفر كل ما نحتاجه (تسجيل دخول، قاعدة بيانات، تحديثات فورية) دون الحاجة لخادم منفصل. هذا يُقلل التعقيد ويُتيح التركيز على منطق التداول والنموذج الرياضي اللذين هما جوهر البحث. في بيئة إنتاجية حقيقية قد نحتاج باك-إند لأسباب أمنية، لكن للمحاكاة البحثية Firebase كافٍ تماماً.

---

**س: كيف تضمن أن صفقة البيع والشراء تتم بشكل صحيح ولا يُخسر أحد؟**

ج: استخدمنا تقنية `writeBatch` في Firestore — مبدأ الذرية. كل خطوات الصفقة (خصم من البائع، إضافة للمشتري، تحديث العرض، كتابة السجل) تُنفَّذ دفعةً واحدة. إما تنجح كلها معاً أو تفشل كلها معاً. لا توجد حالة وسطى يخسر فيها أحد الطرفين.

---

**س: لماذا سجلات المعاملات لا تُحذف أو تُعدَّل؟**

ج: لضمان نزاهة سجل المحاسبة. في أي نظام مالي حقيقي، السجلات يجب أن تكون ثابتة وغير قابلة للتلاعب. حققنا هذا في قواعد Firestore بالنص الصريح: `allow update: if false; allow delete: if false;`

---

**س: كيف تحاكي الإنتاج الشمسي؟ هل الأرقام دقيقة؟**

ج: نعم، نستخدم المعادلة الفيزيائية الحقيقية `P(t) = P_peak × η × I(t) / I_STC`. المعاملات معايَرة لمنطقة أجدابيا (خط عرض 30.75°N) مع عامل موسمي يعكس الفرق بين الصيف والشتاء. الإشعاع العشوائي يحاكي التقلبات الطبيعية للسحاب. الأرقام الناتجة (6–15 كيلوواط يومياً) متوافقة مع دراسات الإشعاع الشمسي الليبية.

---

**س: كيف تحمي بيانات المستخدمين؟**

ج: على ثلاثة مستويات:
1. **Firebase Auth:** تشفير كلمات المرور، جلسات آمنة، لا نخزّن كلمة مرور أبداً
2. **Firestore Rules:** كل مستخدم يرى معاملاته هو فقط، لا يمكن لأحد الكتابة في حساب آخر
3. **المفاتيح السرية:** ملف `.env` لا يُرفع على GitHub أبداً

---

**س: ما الفرق بين هذا المشروع ونظام حقيقي؟**

ج: ثلاثة فروق رئيسية:
1. **المقياس:** هنا 5 منازل، النظام الحقيقي يحتاج آلاف المنازل
2. **العدادات الذكية:** في الواقع يجب ربط النظام بعدادات كهربائية فعلية تقيس الإنتاج والاستهلاك لحظةً بلحظة
3. **الإطار التنظيمي:** يحتاج موافقة شركة الكهرباء والجهات الحكومية

---

**س: ما أصعب جزء في المشروع من ناحية برمجية؟**

ج: بالتأكيد محرك التداول الذري. التحدي كان ضمان اتساق البيانات عند حدوث صفقات متزامنة — إذا اشترى شخصان من نفس العرض في نفس الوقت، يجب ألا يتم البيع لكليهما إذا كانت الكمية غير كافية. `writeBatch` مع فحص الأرصدة في الخطوات 3 و4 يضمن هذا.

---

# الجزء الثالث عشر — كيف تشرح الكود أمام اللجنة

## القسم 22: خارطة الشرح المقترحة (5 دقائق)

```
الدقيقة 1 — المشكلة والحل:
"المشكلة هي هدر الطاقة الشمسية الفائضة. الحل هو سوق رقمي
للتداول المباشر بين المنازل."

الدقيقة 2 — التقنيات:
"React للواجهة، Firebase للمصادقة والبيانات الفورية.
لا يوجد سيرفر — Firebase يتعامل مع كل شيء مباشرةً."

الدقيقة 3 — قاعدة البيانات:
"ثلاث مجموعات: homes (بيانات المنازل)،
offers (عروض السوق)، transactions (سجل دائم لا يُمسح)."

الدقيقة 4 — الجوهر التقني:
"المعادلة الشمسية P=P_peak×η×I/I_STC للمحاكاة.
writeBatch للتداول الذري الثمانية خطوات."

الدقيقة 5 — العرض الحي:
افتح التطبيق وأجرِ محاكاة إنتاج ثم صفقة بيع وشراء.
```

---

## ملاحظة أخيرة

لا تحفظ هذا الكلام حفظاً — **افهمه**. اللجنة تُميّز بين من يفهم ومن يحفظ. إذا سألوك عن أي تفصيل، قل "دعني أوضّح بمثال" ثم اشرح بكلامك الخاص. الثقة في الشرح أهم من الحفظ الحرفي للمصطلحات.

---

*دليل إعداد المشروع — جامعة أجدابيا، قسم هندسة الحاسوب — 2025/2026*
